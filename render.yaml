services:
  - type: web
    name: itguess
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
    plan: free
    branch: main
    healthCheckPath: /
    autoDeploy: true
